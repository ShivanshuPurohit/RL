## Playing Tetris with a DQN Agent

* To train the model, run `python train.py` and to choose your own hyperparameters
run `python train.py --help`. Create a folder to store the models beforehand.
* To test, run `python test.py`
